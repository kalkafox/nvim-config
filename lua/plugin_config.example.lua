-- Settings provided here.
-- Rename this file to plugin_config.lua to use it.

local plugin_config = {
  ['nvim-weather'] = {
    api_key = 'api_key_here',
    zip_code = '00000',
  },
}

return plugin_config
